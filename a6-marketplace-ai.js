/* A6.1 — Marketplace natural-language search foundation
 * Client-side, read-only. Reuses the existing A3.4.1 search control.
 * No schema changes, no writes, no Hallet changes.
 */
(()=>{
 if(window.__A6_AI_SEARCH__)return;window.__A6_AI_SEARCH__=1;
 const app=()=>document.getElementById('app');
 const norm=v=>String(v??'').toLocaleLowerCase('tr-TR').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ı/g,'i');
 const isBrowse=()=>{const a=app();if(!a)return false;const t=String(a.textContent||'');return /İlanlara bak|Gayrimenkul ilanları|Otomobil ilanları/.test(t)&&!!a.querySelector('input');};
 const clean=q=>String(q||'').trim().replace(/\s+/g,' ');
 function interpret(q){q=clean(q);const n=norm(q);const stop=new Set(['ve','ile','olan','olanlar','icin','için','bir','biraz','cok','çok','civarinda','civarında','yaklasik','yaklaşık','kadar','arasi','arası','arasinda','arasında','olsun','istiyorum','arıyorum','ariyorum','bak','bul','bana','lutfen','lütfen','satilik','satılık','ilan','ilanlar']);const tokens=[];n.replace(/\b\d+(?:[\.,]\d+)?\s*(?:milyon|m|m2|metrekare|km|hp|cc|yas|yil|yıllık|yillik)?\b/g,m=>{tokens.push(m.replace(/\s+/g,' '));return m});n.split(/[^a-z0-9çğıöşü+]+/).filter(Boolean).forEach(x=>{if(x.length>=2&&!stop.has(x)&&!/^\d+$/.test(x)&&!tokens.some(t=>t.includes(x)))tokens.push(x)});return {q,tokens:[...new Set(tokens)].slice(0,12)}}
 function findSearch(){const a=app();if(!a)return null;const inputs=[...a.querySelectorAll('input')];return inputs.find(x=>/ara|arama|search/i.test(String(x.placeholder||'')+' '+String(x.getAttribute('aria-label')||'')))||inputs.find(x=>x.type==='search')||inputs[0]||null;}
 function runSearch(q){const s=findSearch();if(!s)return false;const r=interpret(q),text=r.tokens.length?r.tokens.join(' '):r.q;s.value=text;s.dispatchEvent(new Event('input',{bubbles:true}));s.dispatchEvent(new Event('change',{bubbles:true}));const btn=[...app().querySelectorAll('button')].find(b=>/^ara$/i.test(String(b.textContent||'').trim()));if(btn)btn.click();else s.dispatchEvent(new KeyboardEvent('keydown',{key:'Enter',code:'Enter',bubbles:true}));return true;}
 function inject(){if(!isBrowse())return;if(app().querySelector('[data-a6-search]'))return;const host=app(),box=document.createElement('section');box.className='card';box.dataset.a6Search='1';box.style.cssText='margin:10px 0;padding:12px 13px;border-radius:15px';box.innerHTML='<div style="font-weight:900;font-size:14px">✨ Akıllı arama</div><div class="small muted" style="margin-top:3px">İstediğini doğal cümleyle yaz.</div><div style="display:flex;gap:8px;margin-top:9px"><input data-a6-input type="search" placeholder="Örn. Karabağlar 3+1 5 milyon civarı" autocomplete="off" style="flex:1"><button type="button" data-a6-go style="min-width:70px">Ara</button></div><div data-a6-hint class="small muted" style="margin-top:6px;display:none"></div>';const input=box.querySelector('[data-a6-input]'),go=box.querySelector('[data-a6-go]'),hint=box.querySelector('[data-a6-hint]');const submit=()=>{const q=clean(input.value);if(!q)return;const r=interpret(q);hint.textContent=r.tokens.length?'Arama terimleri: '+r.tokens.join(' · '):'Arama metni kullanılıyor';hint.style.display='block';runSearch(q)};go.onclick=submit;input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();submit()}});host.insertBefore(box,host.firstElementChild)}
 const ob=new MutationObserver(inject);ob.observe(document.body,{childList:true,subtree:true});setTimeout(inject,100);setTimeout(inject,500);setTimeout(inject,1200);
})();